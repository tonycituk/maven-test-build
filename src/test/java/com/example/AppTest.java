package com.example;

import static org.junit.Assert.assertTrue;

import org.junit.Test;

/**
 * Unit test for simple App.
 */
public class AppTest {
    /**
     * Rigorous Test :-)
     */
    @Test
    public void shouldAnswerWithTrue() {
        assertTrue(true);
    }
/* 
    @Test
    public void testFraccionSimplificada() {
        Fraccion fraccionA = new Fraccion(8, 4); // 8/4 => 2/1
        Fraccion fraccionB = new Fraccion(2); // 2/1 => already simplified

        assertTrue((fraccionA.getNum() == fraccionB.getNum()) && (fraccionA.getDen() == fraccionB.getDen()));
    } */
}
